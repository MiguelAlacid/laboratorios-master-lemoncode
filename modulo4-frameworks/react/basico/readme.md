Dudas:

Digamos que un pod tiene toda la logica/vista de una "pagina" si en la pagina yo detecto que hay varios componentes entiendo que todos los componentes irian a la carpeta components dentro del pod para usarlo posteriormente en xxx.component.tsx no?

En cuanto al css yo lo he metido todo en styles.css a nivel raiz, debería haber un css dentro de cada pod que haga referencia solo a las cosas que están dentro de ese pod? por ejemplo rickMortyList.styles.css?

Sobre el VM de los pods, es posible que no exista el VM si por ejemplo la api te devuelve lo mismo que quieres en la vista? o aun así lo ideal sería meterlo?
